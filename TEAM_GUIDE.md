# 🧩 高级软件工程课程项目协作指南
**项目名称：基于积木化编程的深度学习教学平台**  
**仓库地址：** [https://github.com/yunshuya/bbvdle](https://github.com/yunshuya/bbvdle)

---

## 🧱 一. 团队开发目标
本项目由高级软件工程课程小组共同完成，旨在在现有深度学习教学平台基础上进行功能改进和优化。  
为了保证多人协作的高效与稳定，请所有成员遵守以下 GitHub 工作流程与分支管理规范。

---

## 👥 二. 团队分工与职责
| 角色 | 职责 |
|------|------|
| 组长（牛珂） | 负责仓库管理、合并分支、发布版本、任务分配 |
| 前端开发（刘倩文、王冉） | 负责界面优化、交互逻辑、组件重构等 |
| 后端开发（牛珂、方妍） | 负责增加网络架构、用户登录管理、自动错误提示等 |
| 测试与文档（朱婧怡） | 负责测试用例、Bug 跟踪、撰写文档与报告 |

> 组长应确保每位成员的任务分支独立，定期 review 并合并。

---

## 🌳 三. 分支管理

仓库采用 **主干 + 开发 + 功能分支** 模型：

| 分支名称 | 说明 |
|-----------|------|
| `main` | 主分支（仅存放稳定、可发布版本） | 仅组长可合并 |
| `dev` | 开发主分支，所有新功能在此合并与测试 | 所有人可提交/合并pulling request |
| `feature-xxx` | 功能分支，每位成员从 `dev` 新建自己的功能分支 | 各自负责 |
| `fix-xxx` | Bug 修复分支 | 各自负责 |
| `docs` | 文档编写专用分支 | 所有人可修改 |


### 🔧 分支命名规范

| 类型 | 命名格式 | 示例 |
|------|------------|------|
| 功能分支 | `feature-模块名` | `feature-login`、`feature-ui-optimize` |
| 修复分支 | `fix-问题名` | `fix-dataset-bug` |
| 文档分支 | `docs-主题` | `docs-readme` |
| 测试分支 | `test-目标` | `test-performance` |

> 📌 命名使用英文小写、短横线分隔，避免空格与中文。

## 🔄 四、开发与合并流程

1. **配置git身份（首次使用时）**
   
    打开命令行（PowerShell 或终端）：
    ```bash
    git config --global user.name "你的GitHub用户名"
    git config --global user.email "你的GitHub注册邮箱"

2. **克隆项目仓库到本地**
   
   在电脑上选择一个合适的位置，右键“Git bash here”，执行：
   ```bash
   git clone https://github.com/yunshuya/bbvdle.git
   ```
   如果出现connection was reset报错：尝试使用SSH直连（具体操作可以自己查AI）

   执行后会在该位置生成项目文件夹

3. **在本地部署运行项目**

   基本流程跟学姐给的类似，大家看一下README.md文件的本地部署流程即可

4. **查看当前分支**
   ```bash
   git branch
   ```

5. **拉取最新开发分支**
   ```bash
   git checkout dev
   git pull origin dev
   ```

6. **创建个人功能分支**
   
   命名规范：feature-你的功能
   ```bash
   git checkout -b feature-yourtask
   ```

7. **进行功能修改与开发**

   1）打开项目（VScode）
   2）修改对应代码文件、文档或前端模块
   3）保存修改后返回终端，输入：
   ```bash
   git status
   ```

8. **提交修改到本地git**
   ```bash
   git add .
   git commit -m "feat: 完成登录模块前端功能"
   ```
   说明：
   
   git add . 表示将所有修改文件放入暂存区
   
   git commit -m 后面是你的提交说明

9. **推送分支到Github仓库**
   ```bash
   git push origin feature-login
   ```
   执行后，GitHub 上会自动创建一个同名分支。

10. **发起Pull Request合并请求**

   1）打开项目网页
   
   2）点击 “Compare & pull request”，或手动进入 Pull requests → New pull request
      
   3）选择
   
   base：dev  （目标分支）
   
   compare：feature-login  （个人功能分支）
   
   4）填写标题和说明
   
   5）点击 Create pull request，然后等待组长或负责人审核并合并

11. **合并至dev和main（组长完成）**

   ```bash
   # 合并至dev
   git checkout dev
   git pull origin dev
   git merge feature-yourtask
   git push origin dev
   # 合并至main
   git checkout main
   git merge dev
   git tag -a v1.0 -m "第一阶段功能完成"
   git push origin main --tags
   ```

12. **同步更新代码**

   每次开始新功能开发之前，请先同步主分支：
   ```bash
   git checkout dev
   git pull origin dev
   ```
   如果出现冲突，请与组长或负责该模块的同学沟通解决。

---

## 五、Git常用命令速查表

| 操作     | 命令                                     |
| ------ | -------------------------------------- |
| 克隆仓库   | `git clone <repo>`                     |
| 查看分支   | `git branch -a`                        |
| 创建分支   | `git checkout -b feature-xxx`          |
| 切换分支   | `git checkout dev`                     |
| 查看状态   | `git status`                           |
| 添加修改   | `git add .`                            |
| 提交修改   | `git commit -m "描述"`                   |
| 推送分支   | `git push origin feature-xxx`          |
| 拉取最新代码 | `git pull origin dev`                  |
| 删除本地分支 | `git branch -d feature-xxx`            |
| 删除远程分支 | `git push origin --delete feature-xxx` |

---

## 六、工作规范提醒

1. **所有组员禁止直接 push 到 main 分支；**

2. **所有代码修改都必须通过 Pull Request 审核合并；**

3. **每次开发前先执行：**
```bash
git pull origin dev
```
确保代码是最新；





   







